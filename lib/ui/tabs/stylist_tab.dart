import 'package:flutter/material.dart';

class StylistTab extends StatelessWidget {
  const StylistTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Stylist"),
    );
  }
}